# set two prefix: 'Ctrl+b' to 'Alt+b'
set-option -g prefix M-b
set-option -g prefix2 C-b
# use mouse
set -g mouse on
# add shortcut for .tmux.conf
bind r source-file ~/.tmux.conf
# split terminals using | and -
unbind '"'
unbind %
bind - split-window -h
bind | split-window -v

